# Flask 核心依赖
Flask==2.3.3
Flask-SQLAlchemy==2.5.1
SQLAlchemy==1.4.53  # 保持主项目版本，try_on 服务已适配
Flask-CORS==4.0.0
Werkzeug==2.3.7
python-dotenv==1.0.0
flask-restx==1.3.0

# 数据库
PyMySQL==1.1.0

# 图片处理
Pillow==10.1.0  # 升级到 try_on 服务需要的版本

# HTTP 客户端
requests==2.31.0
httpx==0.25.1  # try_on 服务需要（用于异步 HTTP 请求）

# 存储服务
oss2==2.18.4

# AI 模型服务
volcengine-python-sdk[ark]>=1.0.0  # 火山引擎方舟 SDK（Seedream 模型）

# 数据验证（try_on 服务需要）
pydantic==2.5.0  # try_on 服务需要（用于参数验证）

# 工具库
python-multipart==0.0.6  # try_on 服务需要（用于文件上传）

# 服务器
gunicorn==23.0.0

# 异步并发（gunicorn gevent worker 需要）
gevent>=1.4

# 缓存
redis==5.0.1

# 测试
pytest==7.4.3
pytest-cov==4.1.0
pytest-asyncio==0.21.1  # try_on 服务需要（用于异步测试） 