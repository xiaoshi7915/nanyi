# 磁盘空间清理报告

**清理时间**: 2026-01-28

## 清理前状态

- **磁盘使用率**: 99% (167G/177G)
- **可用空间**: 2.9G
- **状态**: ⚠️ 严重不足，需要立即清理

## 清理操作

### 1. 清理 /tmp 目录中的 pip-unpack 临时文件
- **清理前**: 6.9GB
- **清理后**: 458MB
- **释放空间**: 约 6.4GB
- **说明**: 删除了 656+ 个 pip 安装时留下的临时解压目录

### 2. 清理系统日志旧文件
- **清理文件**:
  - `/var/log/messages-20260125` (874MB)
  - `/var/log/messages-20260118` (626MB)
  - `/var/log/secure-20260125` (7.6MB)
  - `/var/log/secure-20260118` (7.0MB)
  - `/var/log/secure-20260104` (6.6MB)
  - `/var/log/btmp-20260101` (22MB)
- **释放空间**: 约 1.5GB

### 3. 清理 systemd journal 日志
- **操作**: 保留最近 7 天的日志
- **释放空间**: 部分空间（原占用 371.8MB）

### 4. 清理 Python 缓存文件
- **清理内容**:
  - `__pycache__` 目录
  - `*.pyc` 文件
  - `*.pyo` 文件
- **释放空间**: 约 1MB（虽然不大，但有助于保持代码库整洁）

### 5. 清理旧的 gunicorn 临时文件
- **清理内容**: 7 天前的 `wgunicorn-*` 临时文件

## 清理后状态

- **磁盘使用率**: 94% (159G/177G)
- **可用空间**: 12GB
- **状态**: ✅ 已改善，但仍需关注

## 空间变化

| 项目 | 清理前 | 清理后 | 变化 |
|------|--------|--------|------|
| 磁盘使用率 | 99% | 94% | ↓ 5% |
| 可用空间 | 2.9GB | 12GB | ↑ 9.1GB |
| /tmp 目录 | 6.9GB | 458MB | ↓ 6.4GB |
| /var/log 目录 | ~2GB | 579MB | ↓ ~1.5GB |

## 建议

### 定期清理任务

1. **设置定时清理脚本** (建议每周执行):
   ```bash
   # 清理旧的 pip-unpack 目录
   find /tmp -type d -name "pip-unpack-*" -mtime +1 -exec rm -rf {} \;
   
   # 清理系统日志（保留30天）
   find /var/log -type f -name "*.log-*" -mtime +30 -delete
   
   # 清理 systemd journal（保留7天）
   journalctl --vacuum-time=7d
   ```

2. **配置日志轮转**:
   - 检查 `/etc/logrotate.conf` 配置
   - 确保日志文件定期轮转和删除

3. **监控磁盘使用**:
   - 设置磁盘使用率告警（建议 >85% 时告警）
   - 定期检查大文件: `find / -type f -size +100M 2>/dev/null`

### 当前仍占用较大空间的项目

1. **Docker**: 7.0GB（大部分在使用中，正常）
2. **系统日志**: 579MB（已优化）
3. **项目虚拟环境**: 526MB（正常，必需）

## 注意事项

- ✅ 所有清理操作都是安全的，不会影响系统运行
- ✅ pip-unpack 目录是 pip 安装的临时文件，安装完成后可以安全删除
- ✅ 旧日志文件已归档，删除不会影响当前日志记录
- ⚠️ 建议定期执行清理，避免再次出现磁盘空间不足的情况
